{{#each blogs as |blog|}}
<div class="columns mb-4 project" id="blogbox">
    <div class="column is-5">
        <h2>
        <a class="has-text-success has-text-weight-bold" id="blog-title" href="/blogs/{{blog.id}}">{{blog.title}}</a>
        </h2>
    </div>
    <div class="column is-7">
        <p>
        {{blog.content}}
        </p>
    </div>
</div>
{{/each}}